# clients.conf - daftar NAS (Mikrotik) yang boleh autentikasi ke FreeRADIUS ini
# Baris di bawah ini akan digenerate otomatis oleh script onboarding lokasi baru
# (lihat lib/radius/client.ts -> registerNas), bukan diedit manual satu-satu

# Contoh hasil generate:
# client lokasi-1 {
#     ipaddr = 10.10.1.1
#     secret = <radius_nas_secret dari tabel router>
#     shortname = lokasi-1
#     nas_type = other
# }